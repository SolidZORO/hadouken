extends ./layout/default.jade

block title
    title #{CONFIG.SITE_NAME} - #{CONFIG.SITE_DESC}

block container
    .saya-container
        .saya
            .list
                each data in datas
                    .item
                        h2.title
                            a(href= HOME_PATH + PATH.DAY + data.url + '.html') #{data.title}
                        .date #{data.data_zh}

                        .typo
                            - if (data.banner_thumb)
                                .banner-thumb
                                    img(src= HOME_PATH + data.banner_thumb)
                            - if (data.content_list_html)
                                |!{data.content_list_html}
                            - else
                                |!{data.content_html}
            include ./include/pagination.jade
